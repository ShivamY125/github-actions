name: 08-Using variables

env:
    WORKFLOW_VAR: "I am workflow env var"
    OVERWRITTEN: "I will be overwritten"
    
on:
    push:
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        env:
            JOB_VAR: "I am a job env variables"
            OVERWRITTEN: "I have been overwritten at the job level"
        steps:
           - name: "Print Env variables"
             env:
                 STEP_VAR: "I am a step env var"
                 step_var2: "I am another step var"

             run: |
                      echo "Step env var: ${{env.STEP_VAR}}"
                      echo "Step env var 2: ${{env.step_var2}}"
                      echo "Job env var: ${{env.JOB_VAR}}"
                      echo "Workflow env var: ${{env.WORKFLOW_VAR}}"
                      echo "Overwritten var: ${{env.OVERWRITTEN}}"
           - name: "Overwrite Job Variables"
             env:
                OVERWRITTEN: "I have been overwritten at the same level"
             run: | 
                 echo "Strep env var: ${{env.OVERWRITTEN}}"

